# API Directory

This directory contains API service modules for communicating with the backend.

## Structure
- API client configuration
- Service modules for different endpoints (blueprints, artifacts, etc.)
- Request/response interceptors